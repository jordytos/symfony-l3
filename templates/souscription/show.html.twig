{% extends 'espace-agent/layout.html.twig' %}

{% block title %}Souscription{% endblock %}

{% block body %}
    <h1>Souscription</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ souscription.id }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ souscription.etat }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('souscription_index') }}">Retour</a>

    <a href="{{ path('souscription_edit', {'id': souscription.id}) }}">Modifier</a>

    {{ include('souscription/_delete_form.html.twig') }}
{% endblock %}
